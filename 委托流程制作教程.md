# BGI 委托流程制作教程

## 目录
1. [委托流程概述](#1-委托流程概述)
2. [项目结构](#2-项目结构)
3. [流程文件格式](#3-流程文件格式)
4. [Step 类型详解](#4-step-类型详解)
5. [NPC委托制作](#5-npc委托制作)
6. [Basic委托制作](#6-basic委托制作)
7. [高级功能](#7-高级功能)
8. [调试与测试](#8-调试与测试)

---

## 1. 委托流程概述

### 1.1 什么是委托流程

委托流程是定义委托执行步骤的 JSON 配置文件，告诉脚本如何完成一个特定的每日委托任务。

### 1.2 委托类型

| 类型 | 说明 | 流程位置 |
|------|------|----------|
| **NPC委托** | 以对话交互为主的委托 | `process/NPC/{委托名}/{地点}/process.json` |
| **Basic委托** | 需要战斗的委托 | `process/Basic/{委托名}/` |

### 1.3 执行流程

```
┌─────────────────┐
│   识别委托列表   │
└────────┬────────┘
         ▼
┌─────────────────┐
│  按类型分发执行  │
└────────┬────────┘
         ▼
    ┌────────┐
    │ 对话？  │──是──► 加载对话流程 ──► 执行 step 序列
    └────┬───┘
         │ 否
         ▼
    ┌────────┐
    │ 战斗？  │──是──► 匹配最近地点 ──► 执行 step 序列
    └────┬───┘
         │ 否
         ▼
      [跳过]
```

---

## 2. 项目结构

### 2.1 目录说明

```
process/
├── NPC/                        # NPC委托流程
│   └── {委托名}/
│       └── {地点}/
│           ├── process.json    # 主流程文件
│           └── *.json          # 路径追踪文件
├── Basic/                      # Basic委托流程
│   └── {委托名}/
│       └── {地点}-1/
│           ├── process.json    # 流程文件
│           └── *_path.json     # 路径追踪文件
└── config/                     # 配置数据
    └── commission-branches.json  # 委托分支配置

src/
├── processors/                 # Step 处理器
│   ├── registry.js            # 注册表
│   ├── map-tracking.js        # 地图追踪
│   ├── auto-skip.js           # 对话/剧情
│   └── ...
└── core/
    ├── npc-executor.js        # NPC委托执行器
    └── basic-executor.js      # Basic委托执行器
```

### 2.2 流程文件命名规范

- **NPC委托**: `process/NPC/{委托名}/{地点}/process.json`
- **Basic委托**: `process/Basic/{委托名}/{地点}-1/process.json`
- **路径追踪**: 任意 `.json` 文件名，在 process.json 中引用

---

## 3. 流程文件格式

### 3.1 基本结构

```json
[
  {
    "type": "步骤类型",
    "data": "步骤数据",
    "note": "注释说明"
  },
  ...
]
```

### 3.2 兼容格式

支持两种写法，新格式（推荐）和旧格式（兼容）：

```json
// 新格式（对象）
[
  { "type": "地图追踪", "data": "路径文件.json" },
  { "type": "对话", "data": { "priorityOptions": ["选项1"] } }
]

// 旧格式（字符串）
[
  "路径文件.json",     // 自动识别为 "地图追踪"
  "F"                  // 自动识别为 "对话"
]
```

### 3.3 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `type` | string | 是 | 步骤类型名称 |
| `data` | any | 否 | 步骤数据，根据类型不同而变化 |
| `note` | string | 否 | 注释说明，仅用于阅读 |

---

## 4. Step 类型详解

### 4.1 地图追踪

**用途**: 执行路径追踪文件，自动导航到目标位置

**格式**:
```json
{
  "type": "地图追踪",
  "data": "路径文件.json",
  "note": "导航到目标位置"
}
```

**data 说明**:
- 字符串类型：路径追踪文件名（相对于流程目录）

**示例**:
```json
// 对话委托路径
{ "type": "地图追踪", "data": "语言交流-1.json" }

// 战斗委托路径（自动使用 processDir）
{ "type": "地图追踪", "data": "达达乌帕谷-1_path.json" }
```

---

### 4.2 对话

**用途**: 自动处理 NPC 对话，支持选项选择和跳过剧情

**格式**:
```json
{
  "type": "对话",
  "data": {
    "priorityOptions": ["优先选项1", "优先选项2"],
    "npcWhiteList": ["NPC名称"]
  }
}
```

**data 字段说明**:

| 字段 | 类型 | 说明 |
|------|------|------|
| `priorityOptions` | string[] | 优先选择的对话选项，按顺序匹配 |
| `npcWhiteList` | string[] | NPC 白名单，只与这些 NPC 对话 |

**示例**:
```json
// 简单对话
{ "type": "对话" }

// 带选项优先级的对话
{
  "type": "对话",
  "data": {
    "priorityOptions": ["好的", "没问题"],
    "npcWhiteList": ["马斯克"]
  }
}

// 旧格式兼容
"F"
```

---

### 4.3 等待返回主界面

**用途**: 等待游戏返回到主界面（用于对话/剧情后）

**格式**:
```json
{
  "type": "等待返回主界面",
  "data": {
    "maxWaitTime": 120000,
    "checkInterval": 1000
  }
}
```

**data 字段说明**:

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `maxWaitTime` | number | 120000 | 最大等待时间（毫秒） |
| `checkInterval` | number | 1000 | 检查间隔（毫秒） |

**简化格式**:
```json
{ "type": "等待返回主界面" }
// 或
{ "type": "等待主界面" }
```

---

### 4.4 追踪委托

**用途**: 通过地图图标自动导航到 NPC 位置

**格式**:
```json
{
  "type": "追踪委托",
  "data": {
    "npc": "NPC名称",
    "iconType": "bigmap",
    "autoTalk": false
  }
}
```

**data 字段说明**:

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `npc` | string | "" | 目标 NPC 名称（用于到达检测） |
| `iconType` | string | "bigmap" | 图标类型："bigmap"/"question"/"task" |
| `autoTalk` | boolean | false | 到达后是否自动按 F 对话 |

**示例**:
```json
{
  "type": "追踪委托",
  "data": {
    "npc": "鲁道夫",
    "iconType": "task"
  }
}
```

---

### 4.5 传送

**用途**: 传送到指定坐标

**格式**:
```json
{
  "type": "传送",
  "data": {
    "x": 2297.6,
    "y": -824.5,
    "force": false
  }
}
```

**data 字段说明**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `x` | number | 是 | 目标X坐标（浮点数） |
| `y` | number | 是 | 目标Y坐标（浮点数） |
| `force` | boolean | 否 | 是否强制传送，默认 false |

**示例**:
```json
// 普通传送
{
  "type": "传送",
  "data": {
    "x": 2297.6,
    "y": -824.5
  }
}

// 强制传送
{
  "type": "传送",
  "data": {
    "x": 2297.6,
    "y": -824.5,
    "force": true
  }
}

// 简写别名
{ "type": "tp", "data": { "x": 2297.6, "y": -824.5 } }
```

---

### 4.6 等待

**用途**: 等待指定时间

**格式**:
```json
{
  "type": "等待",
  "data": 2000
}
```

**data 说明**:
- number: 等待毫秒数
- object: `{ "time": 2000 }`
- string: 自动解析为数字

**示例**:
```json
{ "type": "等待", "data": 2000 }      // 等待2秒
{ "type": "等待", "data": { "time": 2000 } }
```

---

### 4.7 按键

**用途**: 执行键盘按键操作

**格式**:
```json
{
  "type": "按键",
  "data": "VK_F"           // 简单按键
}
```

```json
{
  "type": "按键",
  "data": {
    "key": "VK_W",
    "action": "down"        // down/up/press（默认）
  }
}
```

**示例**:
```json
// 简单按键
{ "type": "按键", "data": "F" }

// 组合操作
[
  { "type": "按键", "data": { "key": "VK_W", "action": "down" } },
  { "type": "等待", "data": 1000 },
  { "type": "按键", "data": { "key": "VK_W", "action": "up" } }
]
```

---

### 4.8 键鼠脚本

**用途**: 执行键鼠宏脚本文件

**格式**:
```json
{
  "type": "键鼠脚本",
  "data": "脚本文件.json"
}
```

**示例**:
```json
{
  "type": "键鼠脚本",
  "data": "拾取物品.json"
}
```

---

### 4.9 自动任务

**用途**: 启用/禁用实时自动任务（如 AutoSkip）

**格式**:
```json
{
  "type": "自动任务",
  "data": {
    "action": "enable",      // enable/disable
    "taskType": "AutoSkip",  // 任务类型
    "config": {}             // 可选配置
  }
}
```

**示例**:
```json
// 启用自动剧情
{
  "type": "自动任务",
  "data": {
    "action": "enable",
    "taskType": "AutoSkip"
  }
}

// 禁用所有自动任务
{
  "type": "自动任务",
  "data": {
    "action": "disable"
  }
}
```

---

### 4.10 定时自动战斗

**用途**: 启动自动战斗，持续检测战斗结束

**格式**:
```json
{
  "type": "定时自动战斗",
  "data": {
    "timeout": 30000,    // 战斗超时时间（毫秒）
    "intervals": 5000    // 检测间隔（毫秒）
  }
}
```

**示例**:
```json
{
  "type": "定时自动战斗",
  "data": {
    "timeout": 60000,
    "intervals": 3000
  }
}
```

---

### 4.11 切换队伍

**用途**: 切换到指定队伍

**格式**:
```json
{
  "type": "切换队伍",
  "data": "队伍名称"
}
```

**特殊名称**:
- `"战斗"` - 使用设置中的战斗队伍
- `"元素采集"` - 使用设置中的元素采集队伍

**示例**:
```json
{ "type": "切换队伍", "data": "战斗" }
{ "type": "切换队伍", "data": "一键挖矿" }
```

---

### 4.12 切换角色

**用途**: 在队伍配置界面切换指定位置的角色

**格式**:
```json
{
  "type": "切换角色",
  "data": {
    "character": "角色名",
    "position": 1
  }
}
```

**示例**:
```json
{
  "type": "切换角色",
  "data": {
    "character": "纳西妲",
    "position": 1
  }
}
```

---

### 4.13 地址检测

**用途**: 检测当前委托位置是否匹配预期，根据结果执行不同流程

**格式**:
```json
{
  "type": "地址检测",
  "data": {
    "x": 1000.5,
    "y": 800.3,
    "tolerance": 15
  },
  "run": "后续流程.json"
}
```

**data 字段说明**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `x` | number | 是 | 目标X坐标（浮点数） |
| `y` | number | 是 | 目标Y坐标（浮点数） |
| `tolerance` | number | 否 | 容差范围，默认 15 |

**说明**:
- 比较当前委托位置与目标坐标的距离
- 距离小于容差时，插入 `run` 指定的流程步骤
- 检测结果会写入 `context.locationDetected` 和 `context.detectedPosition`

**示例**:
```json
{
  "type": "地址检测",
  "data": {
    "x": 1000.5,
    "y": 800.3,
    "tolerance": 15
  },
  "run": "特殊处理流程.json"
}

// 使用默认容差
{
  "type": "地址检测",
  "data": {
    "x": 1500.0,
    "y": 2000.0
  },
  "run": "分支A流程.json"
}
```

---

### 4.14 委托描述检测

**用途**: 检测委托描述文本，根据结果执行不同流程

**格式**:
```json
{
  "type": "委托描述检测",
  "data": "目标描述文本",
  "run": "后续流程.json",
  "runType": "process"
}
```

**高级格式**:
```json
{
  "type": "委托描述检测",
  "data": {
    "description": "完整描述",
    "keyword": "关键词",
    "useKeyword": true,        // 是否使用关键词匹配
    "executeFile": "流程.json",
    "runType": "process"       // process/path
  }
}
```

**示例**:
```json
// 检测是否需要采集
{
  "type": "委托描述检测",
  "data": "寻找三份蒲公英籽",
  "run": "采集蒲公英籽process.json"
}
```

---

### 4.15 用户分支选择

**用途**: 根据用户配置文件选择执行不同分支，支持多分支完成进度追踪

**格式**:
```json
{
  "type": "用户分支选择",
  "default": "默认分支名",
  "data": {
    "分支A": { "type": "对话", ... },
    "分支B": { "type": "传送", ... },
    "默认分支名": { "type": "对话", ... }
  }
}
```

**字段说明**:

| 字段 | 位置 | 说明 |
|------|------|------|
| `default` | step 根级 | 默认分支名称，当 selected 为空或全部完成时使用 |
| `data` | step 根级 | 分支定义对象，key 为分支名，value 为 step 配置 |

**配置文件** (`config/commission-branches.json`):
```json
{
  "委托名称": {
    "selected": ["分支A", "分支B"],
    "completed": ["分支A"]
  }
}
```

**选择逻辑**:
1. 如果 `selected` 为空，使用 `step.default`
2. 如果 `selected` 中的分支都已完成（在 `completed` 中），使用 `step.default`
3. 否则，选择 `selected` 中第一个未完成的分支

---

## 5. NPC委托制作

### 5.1 基本流程模板

```json
[
  {
    "type": "地图追踪",
    "data": "步骤1-到达NPC.json",
    "note": "导航到NPC位置"
  },
  {
    "type": "对话",
    "data": {
      "priorityOptions": ["开始任务"],
      "npcWhiteList": ["NPC名称"]
    },
    "note": "与NPC对话"
  },
  {
    "type": "等待返回主界面",
    "note": "等待对话完成"
  },
  {
    "type": "地图追踪",
    "data": "步骤2-前往目标.json",
    "note": "前往任务目标"
  },
  {
    "type": "对话",
    "note": "完成任务"
  },
  {
    "type": "等待返回主界面"
  }
]
```

### 5.2 制作步骤

1. **录制路径追踪**: 使用 BGI 路径追踪录制功能
2. **确定对话选项**: 记录需要优先选择的选项文字
3. **编写流程文件**: 按顺序组合步骤
4. **测试验证**: 运行测试检查流程

### 5.3 完整示例：语言交流

```json
[
  {
    "type": "地图追踪",
    "data": "语言交流-1.json",
    "note": "前往第一个NPC"
  },
  {
    "type": "对话",
    "data": {
      "priorityOptions": ["好的"],
      "npcWhiteList": ["马斯克"]
    },
    "note": "与艾拉·马斯克对话接受任务"
  },
  {
    "type": "等待返回主界面"
  },
  {
    "type": "地图追踪",
    "data": "语言交流-2.json",
    "note": "前往丘丘人营地"
  },
  {
    "type": "对话",
    "note": "与丘丘人对话"
  },
  {
    "type": "地图追踪",
    "data": "语言交流-3.json",
    "note": "返回马斯克"
  },
  {
    "type": "对话",
    "note": "完成任务"
  },
  {
    "type": "等待返回主界面"
  }
]
```

### 5.4 复杂示例：惊喜大礼（多分支）

```json
[
  {
    "type": "地图追踪",
    "data": "去往蒙德大教堂.json"
  },
  {
    "type": "对话",
    "data": {
      "priorityOptions": [""],
      "npcWhiteList": ["进入教堂"]
    },
    "note": "进入教堂"
  },
  {
    "type": "等待返回主界面"
  },
  {
    "type": "追踪委托",
    "data": {
      "npc": "鲁道夫",
      "iconType": "task"
    }
  },
  {
    "type": "对话",
    "data": {
      "priorityOptions": ["开纪念品店的"],
      "npcWhiteList": ["鲁道夫"]
    },
    "note": "选择去找玛乔丽"
  },
  {
    "type": "等待返回主界面"
  },
  {
    "type": "地图追踪",
    "data": "惊喜大礼-1.json"
  },
  {
    "type": "对话",
    "data": {
      "priorityOptions": ["关于礼物", "我付了"],
      "npcWhiteList": ["玛乔丽"]
    }
  },
  {
    "type": "等待返回主界面"
  },
  {
    "type": "地图追踪",
    "data": "去往蒙德大教堂.json"
  },
  {
    "type": "对话",
    "data": {
      "priorityOptions": [""],
      "npcWhiteList": ["进入教堂"]
    }
  },
  {
    "type": "等待返回主界面"
  },
  {
    "type": "追踪委托",
    "data": {
      "npc": "鲁道夫",
      "iconType": "Bigmap"
    }
  },
  {
    "type": "对话",
    "data": {
      "npcWhiteList": ["鲁道夫"]
    }
  },
  {
    "type": "等待返回主界面"
  }
]
```

---

## 6. Basic委托制作

### 6.1 基本流程模板

```json
[
  {
    "type": "传送",
    "data": {
      "x": 156.4,
      "y": 312.8
    },
    "note": "传送到附近锚点"
  },
  {
    "type": "地图追踪",
    "data": "路径文件.json",
    "note": "导航到战斗地点"
  },
  {
    "type": "切换队伍",
    "data": "战斗",
    "note": "切换到战斗队伍"
  },
  {
    "type": "定时自动战斗",
    "data": {
      "timeout": 60000,
      "intervals": 3000
    }
  },
  {
    "type": "等待返回主界面"
  }
]
```

### 6.2 目录结构

```
assets/fight_script/{委托名}/
├── {地点}-1/
│   ├── process.json           # 流程文件
│   └── {地点}-1_path.json     # 路径追踪文件
└── {地点}-2/
    ├── process.json
    └── {地点}-2_path.json
```

### 6.3 匹配机制

战斗委托通过坐标距离匹配最近的流程：

1. 识别委托获取位置坐标
2. 遍历所有地点流程
3. 计算与路径终点的距离
4. 选择距离最小的流程执行

### 6.4 完整示例：攀高危险

```json
[
  {
    "type": "传送",
    "data": {
      "x": 156.4,
      "y": 312.8
    }
  },
  {
    "type": "地图追踪",
    "data": "风啸山坡-1_path.json"
  },
  {
    "type": "切换队伍",
    "data": "战斗"
  },
  {
    "type": "定时自动战斗",
    "data": {
      "timeout": 90000,
      "intervals": 2000
    }
  },
  {
    "type": "等待返回主界面"
  }
]
```

---

## 7. 高级功能

### 7.1 条件流程控制

#### 地址检测实现分支

```json
[
  {
    "type": "地址检测",
    "data": {
      "x": 1500.0,
      "y": 2000.0,
      "tolerance": 20
    },
    "run": "分支A流程.json"
  },
  // 不匹配时继续执行后续步骤
  {
    "type": "地图追踪",
    "data": "默认路径.json"
  }
]
```

#### 委托描述检测实现分支

```json
[
  {
    "type": "委托描述检测",
    "data": "寻找三份蒲公英籽",
    "run": "采集分支.json"
  },
  // 直接交付分支
  {
    "type": "对话",
    "data": { "priorityOptions": ["交付"] }
  }
]
```

### 7.2 用户可配置分支

1. **创建分支配置** (`config/commission-branches.json`):
```json
{
  "愿风带走思念": {
    "selected": ["已采集"],
    "completed": []
  }
}
```

2. **在流程中使用**:
```json
{
  "type": "用户分支选择",
  "default": "需采集",
  "data": {
    "已采集": {
      "type": "对话",
      "data": { "priorityOptions": ["交付"] }
    },
    "需采集": {
      "type": "地图追踪",
      "data": "采集路径.json"
    }
  }
}
```

**工作流程**:
- 首次执行：从 `selected` 中选取第一个未完成的分支（如"已采集"）
- 若"已采集"已完成：使用 `default` 指定的分支（"需采集"）
- 执行完成后：自动将执行的分支添加到 `completed` 中

### 7.3 多个子流程组合

使用 `run` 字段动态插入步骤:

```json
// 主流程
[
  {
    "type": "地图追踪",
    "data": "到达NPC.json"
  },
  {
    "type": "委托描述检测",
    "data": "需采集",
    "run": "采集子流程.json"
  },
  {
    "type": "委托描述检测",
    "data": "需战斗",
    "run": "战斗子流程.json"
  },
  {
    "type": "对话"
  }
]

// 采集子流程.json
[
  {
    "type": "切换队伍",
    "data": "元素采集"
  },
  {
    "type": "地图追踪",
    "data": "采集路径.json"
  },
  {
    "type": "切换角色",
    "data": { "character": "纳西妲", "position": 1 }
  },
  {
    "type": "键鼠脚本",
    "data": "采集.json"
  }
]
```

---

## 8. 调试与测试

### 8.1 测试模式

脚本提供测试模式，可以单独测试某个流程:

```javascript
// 在 test-runner.js 中配置测试
const testConfig = {
  commissionName: "语言交流",
  location: "坠星山谷",
  processFile: "process.json"
};
```

### 8.2 日志级别

使用不同的日志级别帮助调试:

```javascript
log.info("普通信息");   // 正常输出
log.warn("警告信息");   // 注意但不影响执行
log.error("错误信息");  // 执行出错
log.debug("调试信息");  // 仅在调试模式显示
```

### 8.3 常见问题排查

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| 地图追踪失败 | 路径文件不存在 | 检查文件名和路径 |
| 对话卡住 | 选项不匹配 | 更新 priorityOptions |
| 无法识别NPC | 白名单错误 | 检查 npcWhiteList |
| 委托识别失败 | OCR识别问题 | 检查游戏分辨率 |
| 战斗不结束 | 检测超时 | 增加 timeout 值 |

### 8.4 最佳实践

1. **每个步骤后加注释**: 使用 `note` 字段说明用途
2. **合理设置等待时间**: 网络延迟时适当增加等待
3. **优先选项精确匹配**: 使用完整选项文字避免误选
4. **分支处理全覆盖**: 考虑所有可能的分支情况
5. **测试完整流程**: 多次测试确保稳定性

---

## 附录：Step 类型速查表

| Step 类型 | data 格式 | 用途 |
|-----------|-----------|------|
| 地图追踪 | `"文件名.json"` | 执行路径追踪 |
| 对话 | `{priorityOptions, npcWhiteList}` 或 `"F"` | NPC对话 |
| 等待返回主界面 | `{maxWaitTime, checkInterval}` | 等待UI恢复 |
| 追踪委托 | `{npc, iconType, autoTalk}` | 地图导航到NPC |
| 传送 | `{x, y, force?}` | 传送指定坐标 |
| 等待 | `ms` 或 `{time}` | 等待时间 |
| 按键 | `"key"` 或 `{key, action}` | 键盘操作 |
| 键鼠脚本 | `"文件名.json"` | 执行键鼠宏 |
| 自动任务 | `{action, taskType, config}` | 启用/禁用自动任务 |
| 定时自动战斗 | `{timeout, intervals}` | 自动战斗 |
| 切换队伍 | `"队伍名"` | 切换队伍配置 |
| 切换角色 | `{character, position}` | 切换指定位置角色 |
| 地址检测 | `{x, y, tolerance?}` + `run` | 位置分支判断 |
| 委托描述检测 | `"描述"` 或 `{description, keyword}` + `run` | 描述分支判断 |
| 用户分支选择 | `default` + `{分支名: step}` | 用户配置分支 |

---

*文档版本: 1.0*  
*适用版本: AutoCommission-Pro*
