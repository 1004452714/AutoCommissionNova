# BGI 委托流程制作教程

## 目录

1. [委托流程概述](#1-委托流程概述)
2. [项目结构](#2-项目结构)
3. [流程文件格式](#3-流程文件格式)
4. [Step 类型详解](#4-step-类型详解)
5. [NPC委托制作](#5-npc委托制作)
6. [Basic委托制作](#6-basic委托制作)
7. [高级功能](#7-高级功能)
8. [调试与测试](#8-调试与测试)

***

## 1. 委托流程概述

### 1.1 什么是委托流程

委托流程是定义委托执行步骤的 JSON 配置文件，告诉脚本如何完成一个特定的每日委托任务。

### 1.2 委托类型

| 类型          | 说明         | 流程位置                                  |
| ----------- | ---------- | ------------------------------------- |
| **NPC委托**   | 以对话交互为主的委托 | `process/NPC/{委托名}/{地点}/process.json` |
| **Basic委托** | 需要战斗的委托    | `process/Basic/{委托名}/`                |

### 1.3 执行流程

```
┌─────────────────┐
│   识别委托列表   │
└────────┬────────┘
         ▼
┌─────────────────┐
│  按类型分发执行  │
└────────┬────────┘
         ▼
    ┌────────┐
    │ 对话？  │──是──► 加载对话流程 ──► 执行 step 序列
    └────┬───┘
         │ 否
         ▼
    ┌────────┐
    │ 战斗？  │──是──► 匹配最近地点 ──► 执行 step 序列
    └────┬───┘
         │ 否
         ▼
      [跳过]
```

***

## 2. 项目结构

### 2.1 目录说明

```
process/
├── NPC/                        # NPC委托流程
│   └── {委托名}/
│       └── {地点}/
│           ├── process.json    # 主流程文件
│           └── *.json          # 路径追踪文件
├── Basic/                      # Basic委托流程
│   └── {委托名}/
│       └── {地点}-1/
│           ├── process.json    # 流程文件
│           └── *_path.json     # 路径追踪文件
└── config/                     # 配置数据
    └── branches/                # 每个委托一个 {委托名}.json，分支配置

src/
├── processors/                 # Step 处理器
│   ├── registry.js            # 注册表
│   ├── map-tracking.js        # 地图追踪
│   ├── auto-skip.js           # 对话/剧情
│   └── ...
├── loaders/                    # 流程文件加载器（NPC/Basic 共用）
│   ├── npc-process-loader.js
│   └── basic-process-loader.js
└── core/
    ├── npc-executor.js        # NPC委托执行器
    └── basic-executor.js      # Basic委托执行器
```

### 2.2 流程文件命名规范

- **NPC委托**: `process/NPC/{委托名}/{地点}/process.json`
- **Basic委托**: `process/Basic/{委托名}/{地点}-1/process.json`
- **路径追踪**: 任意 `.json` 文件名，在 process.json 中引用

***

## 3. 流程文件格式

### 3.1 基本结构

```json
[
  {
    "type": "步骤类型",
    "data": "步骤数据",
    "note": "注释说明"
  },
  ...
]
```

### 3.2 字段说明

| 字段        | 类型     | 必填 | 说明                                                                          |
| --------- | ------ | -- | --------------------------------------------------------------------------- |
| `type`    | string | 是  | 步骤类型名称                                                                      |
| `data`    | any    | 否  | 步骤数据，根据类型不同而变化                                                              |
| `note`    | string | 否  | 注释说明，会在步骤执行日志中显示                                                           |
| `desc`    | string | 否  | 委托描述条件；当前追踪委托描述包含该文本时才执行本步骤                                                 |
| `loc`     | array  | 否  | 地址条件；支持 `[x, y]`、`[x, y, tolerance]` 或多坐标 `[[x, y], ...]`，任意坐标命中才执行本步骤          |
| `retry`   | number | 否  | 失败时重试次数（不含首次）；瞬时 OCR / 模板匹配抖动适用                                             |
| `retryOn` | string | 否  | 触发重试的条件：`"throw"`（默认，抛错才重试）/ `"return-false"`（仅返回 false 重试）/ `"all"`（两者都重试） |

**重试示例**：

```json
{ "type": "地图追踪", "data": "找NPC.json", "retry": 2 }
{ "type": "委托描述检测", "data": "...", "retry": 3, "retryOn": "all" }
```

> 重试假设步骤幂等。`按键` / 有副作用的 step 启用 `retry` 需谨慎；step 内业务校验失败属配置错误，无论 `retry` 都不会重试。

**通用条件示例**：

```json
{ "type": "地图追踪", "desc": "在第一个地点", "data": "应急补给-5.json" }
{ "type": "按键", "loc": [1000.5, 800.3, 20], "data": "F" }
{ "type": "对话", "loc": [[1000.5, 800.3], [1020, 830, 30]], "data": ["交付材料"] }
```

***

## 4. Step 类型详解

### 4.1 地图追踪

**用途**: 执行路径追踪文件，自动导航到目标位置

**格式**:

```json
{
  "type": "地图追踪",
  "data": "路径文件.json",
  "note": "导航到目标位置"
}
```

**data 说明**:

- 字符串类型：路径追踪文件名（相对于流程目录）

**示例**:

```json
// 对话委托路径
{ "type": "地图追踪", "data": "语言交流-1.json" }

// 战斗委托路径（自动使用 processDir）
{ "type": "地图追踪", "data": "达达乌帕谷-1_path.json" }
```

***

### 4.2 对话

**用途**: 自动处理 NPC 对话，支持选项选择和跳过剧情

**格式**:

```json
{
  "type": "对话",
  "probe": true,
  "data": {
    "priorityOptions": ["优先选项1", "优先选项2"],
    "npcWhiteList": ["NPC名称"]
  }
}
```

**data 字段说明**:

| 字段                | 类型        | 说明                  |
| ----------------- | --------- | ------------------- |
| `priorityOptions` | string\[] | 优先选择的对话选项，按顺序匹配     |
| `npcWhiteList`    | string\[] | NPC 白名单，只与这些 NPC 对话 |

**step 根级字段**:

| 字段      | 类型      | 说明                                                                                                                              |
| ------- | ------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `probe` | boolean | 是否在本次对话内启用分支条件探针扫描。默认 `false`；置 `true` 时翻页前 OCR 当前台词 + 选项区，匹配 `dialog` 探针的 keywords。**仅在你想用这条对话来判定分支条件达成时才开**，否则关键词在非目标对话里出现会误判 |

**示例**:

```json
// 简单对话
{ "type": "对话" }

// 带选项优先级的对话
{
  "type": "对话",
  "data": {
    "priorityOptions": ["好的", "没问题"],
    "npcWhiteList": ["马斯克"]
  }
}
```

***

### 4.3 等待返回主界面

**用途**: 等待游戏返回到主界面（用于对话/剧情后）

**格式**:

```json
{
  "type": "等待返回主界面",
  "data": {
    "maxWaitTime": 120000,
    "checkInterval": 1000
  }
}
```

**data 字段说明**:

| 字段              | 类型     | 默认值    | 说明         |
| --------------- | ------ | ------ | ---------- |
| `maxWaitTime`   | number | 120000 | 最大等待时间（毫秒） |
| `checkInterval` | number | 1000   | 检查间隔（毫秒）   |

**简化格式**:

```json
{ "type": "等待返回主界面" }
// 或
{ "type": "等待主界面" }
```

***

### 4.4 追踪委托

**用途**: 通过地图图标自动导航到 NPC 位置

**格式**:

```json
{
  "type": "追踪委托",
  "data": {
    "npc": "NPC名称",
    "iconType": "bigmap",
    "autoTalk": false
  }
}
```

**data 字段说明**:

| 字段         | 类型      | 默认值      | 说明                              |
| ---------- | ------- | -------- | ------------------------------- |
| `npc`      | string  | ""       | 目标 NPC 名称（用于到达检测）               |
| `iconType` | string  | "bigmap" | 图标类型："bigmap"/"question"/"task" |
| `autoTalk` | boolean | false    | 到达后是否自动按 F 对话                   |

**示例**:

```json
{
  "type": "追踪委托",
  "data": {
    "npc": "鲁道夫",
    "iconType": "task"
  }
}
```

***

### 4.5 传送

**用途**: 传送到指定坐标

**格式**:

```json
{
  "type": "传送",
  "data": {
    "x": 2297.6,
    "y": -824.5,
    "force": false
  }
}
```

**data 字段说明**:

| 字段      | 类型      | 必填 | 说明              |
| ------- | ------- | -- | --------------- |
| `x`     | number  | 是  | 目标X坐标（浮点数）      |
| `y`     | number  | 是  | 目标Y坐标（浮点数）      |
| `force` | boolean | 否  | 是否强制传送，默认 false |

**示例**:

```json
// 普通传送
{
  "type": "传送",
  "data": {
    "x": 2297.6,
    "y": -824.5
  }
}

// 强制传送
{
  "type": "传送",
  "data": {
    "x": 2297.6,
    "y": -824.5,
    "force": true
  }
}

// 简写别名
{ "type": "tp", "data": { "x": 2297.6, "y": -824.5 } }
```

***

### 4.6 等待

**用途**: 等待指定时间

**格式**:

```json
{
  "type": "等待",
  "data": 2000
}
```

**data 说明**:

- number: 等待毫秒数
- object: `{ "time": 2000 }`
- string: 自动解析为数字

**示例**:

```json
{ "type": "等待", "data": 2000 }      // 等待2秒
{ "type": "等待", "data": { "time": 2000 } }
```

***

### 4.7 按键

**用途**: 执行键盘按键操作

**格式**:

```json
{
  "type": "按键",
  "data": "VK_F"           // 简单按键
}
```

```json
{
  "type": "按键",
  "data": {
    "key": "VK_W",
    "action": "down"        // down/up/press（默认）
  }
}
```

**示例**:

```json
// 简单按键
{ "type": "按键", "data": "F" }

// 组合操作
[
  { "type": "按键", "data": { "key": "VK_W", "action": "down" } },
  { "type": "等待", "data": 1000 },
  { "type": "按键", "data": { "key": "VK_W", "action": "up" } }
]
```

***

### 4.8 键鼠脚本

**用途**: 执行键鼠宏脚本文件

**格式**:

```json
{
  "type": "键鼠脚本",
  "data": "脚本文件.json"
}
```

**示例**:

```json
{
  "type": "键鼠脚本",
  "data": "拾取物品.json"
}
```

***

### 4.9 自动任务

**用途**: 启用/禁用实时自动任务（如 AutoSkip）

**格式**:

```json
{
  "type": "自动任务",
  "data": {
    "action": "enable",      // enable/disable
    "taskType": "AutoSkip",  // 任务类型
    "config": {}             // 可选配置
  }
}
```

**示例**:

```json
// 启用自动剧情
{
  "type": "自动任务",
  "data": {
    "action": "enable",
    "taskType": "AutoSkip"
  }
}

// 禁用所有自动任务
{
  "type": "自动任务",
  "data": {
    "action": "disable"
  }
}
```

***

### 4.10 自动战斗

**用途**: 调用 BGI 自动战斗任务，使用当前队伍执行战斗

**格式**:

```json
{
  "type": "自动战斗",
  "data": {
    "timeout": 120    // 战斗超时时间（秒）
  }
}
```

**说明**:

- 战斗策略从队伍配置读取：委托级战斗策略优先，否则使用全局战斗策略
- 未配置策略时使用 BGI 默认的“根据队伍自动选择”
- 本步骤不会切换队伍或修改队伍角色，进入战斗前需要自行确保当前队伍正确
- 未传 `timeout` 时使用 BGI 自动战斗配置中的默认超时时间

**示例**:

```json
{
  "type": "自动战斗",
  "data": {
    "timeout": 120
  }
}
```

***

### 4.11 切换队伍

**用途**: 按用途切换队伍；队伍名称在配置面板中设置

**格式**:

```json
{
  "type": "切换队伍",
  "data": "战斗"
}
```

**可用值**:

- `"战斗"` - 使用配置面板中的战斗队伍
- `"元素采集"` - 使用配置面板中的元素采集队伍

当对应配置使用角色名模式时，会先切换到配置的自定义承载队伍，再更换 1-4 号位角色。`data` 不支持直接填写任意队伍名。

**示例**:

```json
{ "type": "切换队伍", "data": "战斗" }
{ "type": "切换队伍", "data": "元素采集" }
```

***

### 4.12 切换角色

**用途**: 在队伍配置界面一次性切换 1-4 个槽位的角色

**格式**: `data` 是 `{ "1": "角色名", "2": "角色名", ... }`，键为槽位编号（1-4 的字符串），值为角色名

```json
{
  "type": "切换角色",
  "data": {
    "1": "芙宁娜",
    "2": "钟离",
    "3": "枫原万叶",
    "4": "那维莱特"
  }
}
```

**说明**:

- 角色名必须在 `Data/avatar/avatar_info.json` 中存在；任一角色缺失则整步跳过（不会留下半成品 UI）
- 角色头像模板需要放在 `Data/RecognitionObject/avatar/{角色名}/` 目录下
- 单个角色 SIFT 找不到时最多重试 5 轮，超过仍未找到则跳过此角色继续下一个

***

### 4.13 地址检测

**用途**: 检测当前委托位置是否匹配预期，根据结果执行不同流程

**格式**:

```json
{
  "type": "地址检测",
  "data": {
    "x": 1000.5,
    "y": 800.3,
    "tolerance": 15
  },
  "run": "后续流程.json"
}
```

**data 字段说明**:

| 字段          | 类型     | 必填 | 说明         |
| ----------- | ------ | -- | ---------- |
| `x`         | number | 是  | 目标X坐标（浮点数） |
| `y`         | number | 是  | 目标Y坐标（浮点数） |
| `tolerance` | number | 否  | 容差范围，默认 15 |

**说明**:

- 比较当前委托位置与目标坐标的距离
- 距离小于容差时，插入 `run` 指定的流程步骤
- 检测结果会写入 `context.locationDetected` 和 `context.detectedPosition`
- 与通用字段 `loc` 的区别：`loc` 只控制当前步骤是否执行；`地址检测` 用于命中后插入 `run` 指定的子流程

**示例**:

```json
{
  "type": "地址检测",
  "data": {
    "x": 1000.5,
    "y": 800.3,
    "tolerance": 15
  },
  "run": "特殊处理流程.json"
}

// 使用默认容差
{
  "type": "地址检测",
  "data": {
    "x": 1500.0,
    "y": 2000.0
  },
  "run": "分支A流程.json"
}
```

***

### 4.14 委托描述检测

**用途**: 检测委托描述文本，根据结果执行不同流程

**格式**:

```json
{
  "type": "委托描述检测",
  "data": "目标描述文本",
  "run": "后续流程.json",
  "runType": "process"
}
```

**高级格式**:

```json
{
  "type": "委托描述检测",
  "data": {
    "description": "完整描述",
    "keyword": "关键词",
    "useKeyword": true,        // 是否使用关键词匹配
    "executeFile": "流程.json",
    "runType": "process"       // process/path
  }
}
```

**示例**:

```json
// 检测是否需要采集
{
  "type": "委托描述检测",
  "data": "寻找三份蒲公英籽",
  "run": "采集蒲公英籽process.json"
}
```

***

### 4.15 用户分支选择

**用途**: 根据用户配置文件选择执行不同分支，支持"成就分支"完成进度追踪与"偏好分支"无状态选择

**格式**:

```json
{
  "type": "用户分支选择",
  "default": "默认分支名",
  "data": {
    "分支A": { "type": "对话", ... },
    "分支B": { "type": "传送", ... },
    "默认分支名": { "type": "对话", ... }
  },
  "note": "根据用户配置选择分支"
}
```

**字段说明**:

| 字段        | 位置      | 说明                               |
| --------- | ------- | -------------------------------- |
| `default` | step 根级 | step 级默认分支（用户配置 default 缺省时回退）   |
| `data`    | step 根级 | 分支定义对象，key 为分支标识，value 为 step 配置 |
| `note`    | step 根级 | 注释说明，仅用于阅读                       |

**配置文件** —— 每个委托一个文件 `process/config/branches/{委托名}.json`：

```json
// process/config/branches/餐品订单.json
{
  "type": "achievement",
  "descriptions": {
    "branch-1": "正常送餐",
    "branch-2": "偷吃餐品"
  },
  "conditions": {
    "branch-2": { "type": "dialog", "keywords": ["偷偷吃了"] }
  },
  "completed": [],
  "default": "branch-1"
}
```

> 新增带分支的委托：在 `process/config/branches/` 下扔一个 `{委托名}.json` 即可，启动期 loader 自动发现并合并到内存 composite，无需改任何代码。

**配置文件字段说明**:

| 字段             | 类型        | 说明                                         |
| -------------- | --------- | ------------------------------------------ |
| `type`         | string    | 配置类型（UI 标签，如 "achievement"）                |
| `descriptions` | object    | **分支唯一定义**：key=分支标识，value=显示文字。UI 与日志均从这里取 |
| `conditions`   | object    | 标记"成就分支"并定义探测达成方式；不在此声明的分支为偏好分支            |
| `completed`    | string\[] | 已永久解锁的成就分支；运行时自动维护                         |
| `default`      | string    | 没有成就分支可跑时的兜底（用户级 default，优先于 step.default） |

**分支两种类型**:

1. **成就分支**：在 `conditions[branchKey]` 中有声明 → 按声明顺序进入成就队列，命中条件后写入 `completed`，永久跳过
2. **偏好分支**：在 `descriptions` 中存在但不在 `conditions` 中 → 永远不进 `completed`，只能通过 `default` 选中

**conditions 字段格式（探针 schema）**:

`conditions[branchKey].type` 决定使用哪个探针；各探针自带 schema 校验、调度点与实现。当前内置三种：

| `type`          | 必填子字段                | 调度时机                   | 用途                     |
| --------------- | -------------------- | ---------------------- | ---------------------- |
| `"dialog"`      | `keywords: string[]` | 对话 step OCR 结果就绪时      | 扫描对话/选项文字，命中关键词即达成     |
| `"achievement"` | `name: string`       | 流程内显式调用 `成就检测` step    | 打开成就界面搜索 `name` 判断是否解锁 |
| `"completion"`  | （无）                  | 委托 `isCompleted` 检测通过后 | 委托能跑完即视为分支达成           |

新增探针类型：在 `src/probes/` 新增一个 `xxx-probe.js`（用 `defineProbe` 包装并实现需要的钩子），然后在 `src/probes/index.js` 顶部 import + 加进 `probes` 数组。架构与 step processor 完全对称。详见 `src/probes/define-probe.js` 头部注释。

**选择逻辑**:

```
queue = Object.keys(conditions).filter(k => !completed.includes(k))   // 按声明顺序
if queue 非空:
    跑 queue[0]
else:
    跑 default（commissionConfig.default 优先，step.default 次之）
```

**分支锁定**:
一个委托执行期内，多次 `用户分支选择` step 自动选定**同一分支**——首次决策后写 `context.activeBranch`，后续 step 直接复用。

**日志打印**:

- 配置了 `descriptions` 时打印描述值（如 `偷吃餐品`）
- 未配置时打印分支 key（如 `branch-2`）

**示例**:

```json
// process.json
{
  "type": "用户分支选择",
  "default": "branch-1",
  "data": {
    "branch-1": {
      "type": "等待返回主界面",
      "note": "正常送餐"
    },
    "branch-2": {
      "type": "对话",
      "data": { "priorityOptions": ["偷吃"], "npcWhiteList": ["莎拉"] },
      "note": "偷吃餐品"
    }
  },
  "note": "根据用户配置选择分支"
}

// process/config/branches/餐品订单.json
{
  "type": "achievement",
  "descriptions": {
    "branch-1": "正常送餐",
    "branch-2": "偷吃餐品"
  },
  "conditions": {
    "branch-2": { "type": "dialog", "keywords": ["偷偷吃了"] }
  },
  "completed": [],
  "default": "branch-1"
}
```

**执行流程**:

1. 用户在 `process/config/branches/{委托名}.json` 配置分支 + conditions
2. 脚本首次进入 `用户分支选择` step：
   - 加载并缓存配置到 `context.branchConfigCache`
   - 按 `Object.keys(conditions)` 顺序找第一个未完成的成就分支；无则用 default
   - 写 `context.activeBranch` 与 `context.branchCondition` 锁定
3. 执行选定分支的嵌套 step
4. 探针 step（对话/成就检测）按 `context.branchCondition` 判断是否达成，写 `context.branchConditionMet`
5. 委托成功完成后，`updateBranchCompletion` 在 `branchCondition 非空 && branchConditionMet === true` 时把 `activeBranch` 写入 `completed`
6. 偏好分支（无 condition）永远不进 completed，每天都可重新跑

***

### 4.16 执行子流程

**用途**: 加载并执行一个子流程文件（相当于 `include`），把子流程里的 step 依次插入到当前 step 之后

**格式**:

```json
{
  "type": "执行子流程",
  "data": {
    "path": "子流程文件.json"
  },
  "note": "执行子流程"
}
```

**data 字段说明**:

| 字段     | 类型     | 必填 | 说明                                        |
| ------ | ------ | -- | ----------------------------------------- |
| `path` | string | 是  | 子流程文件相对路径（由 `context.resolveResource` 解析） |

**说明**:

- 子流程文件格式与 `process.json` 完全一致（step 数组）
- 路径解析规则与 `地图追踪` / `键鼠脚本` 等其他相对路径字段一致：
  - NPC 委托 → `process/NPC/{委托名}/{地点}/{path}`
  - Basic 委托 → `{processDir}/{path}`
- 与 `委托描述检测` / `地址检测` 命中后的 splice 机制相同，区别是**无前置条件**，永远执行
- 文件读取/解析失败、内容不是数组 → 记 error 并继续，不影响后续 step
- 子流程内可以再嵌套 `执行子流程`，但要自己避免循环引用

**示例**:

```json
// 主流程：把一段常用的 "对话 + 等待主界面" 抽出来复用
[
  {
    "type": "追踪委托",
    "data": { "npc": "凯瑟琳", "iconType": "task" }
  },
  {
    "type": "执行子流程",
    "data": { "path": "公共-对话并返回.json" },
    "note": "复用对话子流程"
  },
  {
    "type": "地图追踪",
    "data": "去交付.json"
  }
]

// 公共-对话并返回.json
[
  { "type": "对话" },
  { "type": "等待返回主界面" }
]
```

***

### 4.17 成就检测

**用途**: 调用 [achievement-probe.runExplicit](src/probes/achievement-probe.js)：打开派蒙菜单 → 进入成就界面 → 搜索成就名 → 判断是否解锁。解锁则置 `context.branchConditionMet = true`。

**格式**:

```json
{
  "type": "成就检测",
  "note": "检测成就是否已解锁"
}
```

**data 字段说明（一般不写）**:

| 字段     | 类型     | 必填 | 说明                                                                                |
| ------ | ------ | -- | --------------------------------------------------------------------------------- |
| `name` | string | 否  | 成就名 override；**生产配置不应填这里**，名字应放在 `process/config/branches/{委托名}.json` 的 `conditions[branchKey].name` 中。`data.name` 仅作测试 / 临时覆盖用 |

**成就名的单一来源原则**: 同一个名字不要在 process.json 和 `branches/{委托名}.json` 两边维护，会漂移。生产配置只写在 conditions 里，由 `用户分支选择` step 注入到 `context.branchCondition.name`，本 step 自动取用。

**典型用法（搭配 用户分支选择 的 achievement 分支）**:

```json
// process/config/branches/委托名称.json
{
  "descriptions": {
    "branch-1": "正常",
    "branch-2": "偷吃"
  },
  "conditions": {
    "branch-2": { "type": "achievement", "name": "这不是应急食品" }
  },
  "default": "branch-1"
}

// process.json — 在成就分支 step 内放一个 成就检测
{
  "type": "用户分支选择",
  "data": {
    "branch-1": { "type": "等待返回主界面" },
    "branch-2": {
      "type": "执行子流程",
      "data": { "path": "branch-2-子流程.json" }
    }
  }
}

// branch-2-子流程.json — 子流程最后加 成就检测
[
  { "type": "地图追踪", "data": "..." },
  { "type": "对话", "data": { "priorityOptions": ["偷吃"] } },
  { "type": "成就检测", "note": "name 自动从 branchCondition 取" }
]
```

**说明**:

- **检测必须放在分支 step 内部**，不能放外面 —— 否则偏好分支跑完也会触发成就分支的成就检测
- 内部走 `dispatchExplicit(context, "achievement", step.data)`，会校验当前 condition.type === "achievement"，不匹配静默跳过（防止流程作者把本 step 放到非成就分支里）
- 失败处理：swallow=true，UI 异常 / 识别失败时 `branchConditionMet` 保持 false（不被错误标记完成）
- 命中后委托完成时自动写入 `completed`，永久跳过该分支

---

### 4.18 分支探针体系（架构参考）

`成就检测` step 只是探针体系的入口之一。完整的探针 = `process/config/branches/{委托名}.json` 中 `conditions[branchKey].type` + `src/probes/` 下对应 probe 文件的钩子实现。当前内置：

| `type` | 触发时机 | 必填字段 | 用法 |
|---|---|---|---|
| `"dialog"` | `对话` step OCR 结果就绪时（需要 `step.probe === true` 才扫描，见 4.2） | `keywords: string[]` | 流程内任意 `对话` step 加 `"probe": true` 即可 |
| `"achievement"` | 流程内显式 `成就检测` step（4.17） | `name: string` | 在成就分支 step 内放 `成就检测` step |
| `"completion"` | 委托被识别为完成时自动触发 | （无） | **无需流程内放任何 step**；只要 conditions 里写了，委托一完成就视为达成 |

**`completion` 完整示例**：

```json
// process/config/branches/某委托.json
{
  "descriptions": { "branch-1": "无脑跑通就行" },
  "conditions": {
    "branch-1": { "type": "completion" }
  },
  "default": "branch-1"
}
```

`process.json` 不需要额外探针 step，委托能跑完就自动写 `completed`，下次永久跳过。

**扩展新探针类型**：在 `src/probes/` 新增 `xxx-probe.js`（用 `defineProbe` 包装并实现需要的钩子），然后在 `src/probes/index.js` 顶部 import + 加进 `probes` 数组。架构与 step processor 完全对称。详见 `src/probes/define-probe.js` 头部注释。

---

### 4.19 使用道具

**用途**: 打开背包，切到指定分页，按候选列表顺序找第一个背包里实际拥有的道具使用一次

**格式**:

```json
{
  "type": "使用道具",
  "data": {
    "tab": "食物",
    "items": ["蒙德土豆饼", "土豆饼", "甜甜花酿鸡"]
  },
  "note": "找到列表里任意一个就用一次，不会全用"
}
```

**data 字段说明**:

| 字段      | 类型        | 必填 | 说明                                                  |
| ------- | --------- | -- | --------------------------------------------------- |
| `tab`   | string    | 是  | 背包分页名称（武器 / 圣遗物 / 材料 / 任务 / 小道具 / 食物 / ...）   |
| `items` | string\[] | 是  | 候选道具名数组；按声明顺序找**第一个**背包里实际拥有的道具，使用一次。**不会**消耗光全部库存，也**不会**把列表里所有道具都用一遍 |

**用途场景**:

- "有 A 用 A，没 A 用 B" 的兜底：先试高级 / 优先食材，没有就退化到低级
- 流程需要触发"使用过某类道具"的剧情条件，库存不固定哪种道具

**说明**:

- 道具名要精确匹配；OCR 抖动可能需要标准化处理（参见 [src/utils/text-utils.js](src/utils/text-utils.js)）
- swallow=true：UI 异常 / 一个候选都没找到时只 log.warn，不阻断后续 step。若需严格失败语义，在 step 上加 `"retry": N`
- 内部步骤：打开背包 → 切分页 → 按 items 顺序定位首个命中 → 点击使用 → 收尾返回主界面

***

## 5. NPC委托制作

### 5.1 基本流程模板

```json
[
  {
    "type": "地图追踪",
    "data": "步骤1-到达NPC.json",
    "note": "导航到NPC位置"
  },
  {
    "type": "对话",
    "data": {
      "priorityOptions": ["开始任务"],
      "npcWhiteList": ["NPC名称"]
    },
    "note": "与NPC对话"
  },
  {
    "type": "等待返回主界面",
    "note": "等待对话完成"
  },
  {
    "type": "地图追踪",
    "data": "步骤2-前往目标.json",
    "note": "前往任务目标"
  },
  {
    "type": "对话",
    "note": "完成任务"
  },
  {
    "type": "等待返回主界面"
  }
]
```

### 5.2 制作步骤

1. **录制路径追踪**: 使用 BGI 路径追踪录制功能
2. **确定对话选项**: 记录需要优先选择的选项文字
3. **编写流程文件**: 按顺序组合步骤
4. **测试验证**: 运行测试检查流程

### 5.3 完整示例：语言交流

```json
[
  {
    "type": "地图追踪",
    "data": "语言交流-1.json",
    "note": "前往第一个NPC"
  },
  {
    "type": "对话",
    "data": {
      "priorityOptions": ["好的"],
      "npcWhiteList": ["马斯克"]
    },
    "note": "与艾拉·马斯克对话接受任务"
  },
  {
    "type": "等待返回主界面"
  },
  {
    "type": "地图追踪",
    "data": "语言交流-2.json",
    "note": "前往丘丘人营地"
  },
  {
    "type": "对话",
    "note": "与丘丘人对话"
  },
  {
    "type": "地图追踪",
    "data": "语言交流-3.json",
    "note": "返回马斯克"
  },
  {
    "type": "对话",
    "note": "完成任务"
  },
  {
    "type": "等待返回主界面"
  }
]
```

### 5.4 复杂示例：惊喜大礼（多分支）

```json
[
  {
    "type": "地图追踪",
    "data": "去往蒙德大教堂.json"
  },
  {
    "type": "对话",
    "data": {
      "priorityOptions": [""],
      "npcWhiteList": ["进入教堂"]
    },
    "note": "进入教堂"
  },
  {
    "type": "等待返回主界面"
  },
  {
    "type": "追踪委托",
    "data": {
      "npc": "鲁道夫",
      "iconType": "task"
    }
  },
  {
    "type": "对话",
    "data": {
      "priorityOptions": ["开纪念品店的"],
      "npcWhiteList": ["鲁道夫"]
    },
    "note": "选择去找玛乔丽"
  },
  {
    "type": "等待返回主界面"
  },
  {
    "type": "地图追踪",
    "data": "惊喜大礼-1.json"
  },
  {
    "type": "对话",
    "data": {
      "priorityOptions": ["关于礼物", "我付了"],
      "npcWhiteList": ["玛乔丽"]
    }
  },
  {
    "type": "等待返回主界面"
  },
  {
    "type": "地图追踪",
    "data": "去往蒙德大教堂.json"
  },
  {
    "type": "对话",
    "data": {
      "priorityOptions": [""],
      "npcWhiteList": ["进入教堂"]
    }
  },
  {
    "type": "等待返回主界面"
  },
  {
    "type": "追踪委托",
    "data": {
      "npc": "鲁道夫",
      "iconType": "Bigmap"
    }
  },
  {
    "type": "对话",
    "data": {
      "npcWhiteList": ["鲁道夫"]
    }
  },
  {
    "type": "等待返回主界面"
  }
]
```

***

## 6. Basic委托制作

### 6.1 基本流程模板

```json
[
  {
    "type": "传送",
    "data": {
      "x": 156.4,
      "y": 312.8
    },
    "note": "传送到附近锚点"
  },
  {
    "type": "地图追踪",
    "data": "路径文件.json",
    "note": "导航到战斗地点"
  },
  {
    "type": "切换队伍",
    "data": "战斗",
    "note": "切换到战斗队伍"
  },
  {
    "type": "自动战斗",
    "data": {
      "timeout": 120
    }
  },
  {
    "type": "等待返回主界面"
  }
]
```

### 6.2 目录结构

```
assets/fight_script/{委托名}/
├── {地点}-1/
│   ├── process.json           # 流程文件
│   └── {地点}-1_path.json     # 路径追踪文件
└── {地点}-2/
    ├── process.json
    └── {地点}-2_path.json
```

### 6.3 匹配机制

战斗委托通过坐标距离匹配最近的流程：

1. 识别委托获取位置坐标
2. 遍历所有地点流程
3. 计算与路径终点的距离
4. 选择距离最小的流程执行

### 6.4 完整示例：攀高危险

```json
[
  {
    "type": "传送",
    "data": {
      "x": 156.4,
      "y": 312.8
    }
  },
  {
    "type": "地图追踪",
    "data": "风啸山坡-1_path.json"
  },
  {
    "type": "切换队伍",
    "data": "战斗"
  },
  {
    "type": "自动战斗",
    "data": {
      "timeout": 150
    }
  },
  {
    "type": "等待返回主界面"
  }
]
```

***

## 7. 高级功能

### 7.1 条件流程控制

#### 地址检测实现分支

```json
[
  {
    "type": "地址检测",
    "data": {
      "x": 1500.0,
      "y": 2000.0,
      "tolerance": 20
    },
    "run": "分支A流程.json"
  },
  // 不匹配时继续执行后续步骤
  {
    "type": "地图追踪",
    "data": "默认路径.json"
  }
]
```

#### 委托描述检测实现分支

```json
[
  {
    "type": "委托描述检测",
    "data": "寻找三份蒲公英籽",
    "run": "采集分支.json"
  },
  // 直接交付分支
  {
    "type": "对话",
    "data": { "priorityOptions": ["交付"] }
  }
]
```

### 7.2 用户可配置分支

1. **创建分支配置** —— 在 `process/config/branches/` 下新建 `{委托名}.json`：

```json
// process/config/branches/餐品订单.json
{
  "type": "achievement",
  "descriptions": {
    "branch-1": "正常送餐",
    "branch-2": "偷吃餐品"
  },
  "conditions": {
    "branch-2": { "type": "dialog", "keywords": ["偷偷吃了"] }
  },
  "completed": [],
  "default": "branch-1"
}
```

1. **在流程中使用**:

```json
{
  "type": "用户分支选择",
  "default": "branch-1",
  "data": {
    "branch-1": {
      "type": "等待返回主界面",
      "note": "正常送餐"
    },
    "branch-2": {
      "type": "对话",
      "data": { "priorityOptions": ["偷吃"], "npcWhiteList": ["莎拉"] },
      "note": "偷吃餐品"
    }
  },
  "note": "根据用户配置选择分支"
}
```

**工作流程**:

- 首次执行：从 `conditions` 中按声明顺序选第一个未在 `completed` 中的分支（成就分支）
- 如果没有未完成的成就分支：使用 `default`（用户配置优先，step 其次）
- 成就分支被探针 step 检测到条件达成 → 委托完成后自动写入 `completed`，下次跑跳过
- 偏好分支（descriptions 中存在但 conditions 未声明）：永远不写 `completed`，只能通过 `default` 选中

**两种探针类型**:

- `"dialog"` + `keywords`：对话 step 在 OCR 结果中扫关键词（**需要在那条 对话 step 上加 `"probe": true`**）
- `"achievement"` + `name`：在成就分支内放 `成就检测` step 显式触发
- `"completion"`：委托被识别为完成时自动触发（无需流程内放探针 step）

详见 [4.15 用户分支选择](#415-用户分支选择) 与 [4.18 分支探针体系](#418-分支探针体系架构参考)。

**日志输出**:

- 日志将打印 `descriptions` 中的描述值（如 `选择第一个未完成的成就分支: 偷吃餐品`）
- 如果未配置 `descriptions`，则打印分支 key

### 7.3 多个子流程组合

使用 `run` 字段动态插入步骤:

```json
// 主流程
[
  {
    "type": "地图追踪",
    "data": "到达NPC.json"
  },
  {
    "type": "委托描述检测",
    "data": "需采集",
    "run": "采集子流程.json"
  },
  {
    "type": "委托描述检测",
    "data": "需战斗",
    "run": "战斗子流程.json"
  },
  {
    "type": "对话"
  }
]

// 采集子流程.json
[
  {
    "type": "切换队伍",
    "data": "元素采集"
  },
  {
    "type": "地图追踪",
    "data": "采集路径.json"
  },
  {
    "type": "切换角色",
    "data": { "1": "纳西妲", "2": "夜兰", "3": "万叶", "4": "班尼特" }
  },
  {
    "type": "键鼠脚本",
    "data": "采集.json"
  }
]
```

***

## 8. 调试与测试

### 8.1 测试模式

脚本提供测试模式，可以单独测试某个流程:

```javascript
// 在 test-runner.js 中配置测试
const testConfig = {
  commissionName: "语言交流",
  location: "坠星山谷",
  processFile: "process.json"
};
```

### 8.2 日志级别

使用不同的日志级别帮助调试:

```javascript
log.info("普通信息");   // 正常输出
log.warn("警告信息");   // 注意但不影响执行
log.error("错误信息");  // 执行出错
log.debug("调试信息");  // 仅在调试模式显示
```

### 8.3 常见问题排查

| 问题      | 可能原因    | 解决方案               |
| ------- | ------- | ------------------ |
| 地图追踪失败  | 路径文件不存在 | 检查文件名和路径           |
| 对话卡住    | 选项不匹配   | 更新 priorityOptions |
| 无法识别NPC | 白名单错误   | 检查 npcWhiteList    |
| 委托识别失败  | OCR识别问题 | 检查游戏分辨率            |
| 战斗不结束   | 检测超时    | 增加 timeout 值       |

### 8.4 最佳实践

1. **每个步骤后加注释**: 使用 `note` 字段说明用途
2. **合理设置等待时间**: 网络延迟时适当增加等待
3. **优先选项精确匹配**: 使用完整选项文字避免误选
4. **分支处理全覆盖**: 考虑所有可能的分支情况
5. **测试完整流程**: 多次测试确保稳定性

***

## 附录：Step 类型速查表

| Step 类型 | data 格式                                   | 用途                                  |
| ------- | ----------------------------------------- | ----------------------------------- |
| 地图追踪    | `"文件名.json"`                              | 执行路径追踪                              |
| 对话      | `{priorityOptions, npcWhiteList}` 或 `"F"`；step 根级可加 `"probe": true` 开启分支条件扫描 | NPC对话                               |
| 等待返回主界面 | `{maxWaitTime, checkInterval}`            | 等待UI恢复                              |
| 追踪委托    | `{npc, iconType, autoTalk}`               | 地图导航到NPC                            |
| 传送      | `{x, y, force?}`                          | 传送指定坐标                              |
| 等待      | `ms` 或 `{time}`                           | 等待时间                                |
| 按键      | `"key"` 或 `{key, action}`                 | 键盘操作                                |
| 键鼠脚本    | `"文件名.json"`                              | 执行键鼠宏                               |
| 自动任务    | `{action, taskType, config}`              | 启用/禁用自动任务                           |
| 自动战斗    | `{timeout}`                               | 调用 BGI 自动战斗                         |
| 切换队伍    | `"战斗"` 或 `"元素采集"`                      | 按用途切换队伍配置                          |
| 切换角色    | `{"1": "角色名", "2": "角色名", ...}`           | 一次切换 1-4 个槽位的角色                     |
| 地址检测    | `{x, y, tolerance?}` + `run`              | 位置分支判断                              |
| 委托描述检测  | `"描述"` 或 `{description, keyword}` + `run` | 描述分支判断                              |
| 用户分支选择  | `default` + `data` + `{分支名: step}`        | 用户配置分支（成就/偏好）                       |
| 执行子流程  | `{path}`                                  | 无条件加载并 splice 子流程文件                 |
| 成就检测    | （一般不填 data）                              | 触发 achievement 探针 — 检索成就解锁状态写 branchConditionMet |
| 使用道具    | `{tab, items[]}`                          | 打开背包指定分页，按候选列表挑首个拥有的道具使用一次 |

***

*文档版本: 1.0*\
*适用版本: AutoCommission-Pro*
